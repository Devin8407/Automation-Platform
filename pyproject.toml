[project]
name = "automation-platform"
version = "0.1.0"
description = "A plugin-based workflow automation platform."
readme = "README.md"
requires-python = ">=3.14,<3.15"

authors = [
    { name = "Devin MacKay" }
]
dependencies = [
    "psycopg[binary]>=3.3.4",
    "sqlalchemy>=2.0.51",
]


[dependency-groups]
dev = [
    "mypy>=2.3.0",
    "pre-commit>=4.6.0",
    "pytest>=9.1.1",
    "pytest-cov>=7.1.0",
    "ruff>=0.15.21",
]


[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"


[tool.ruff]
target-version = "py314"
line-length = 100


[tool.ruff.lint]
select = ["E", "F", "I"]


[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]


[tool.coverage.report]
exclude_also = [
    "@abstractmethod",
]

[project.scripts]
automation-worker = "automation_platform.runtime.worker.bootstrap:run_worker"
